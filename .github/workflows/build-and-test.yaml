name: Build and Test

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v5

      - name: Install JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      # bin/flix downloads the pinned compiler into .flix/, and dependency
      # resolution populates lib/. Both are keyed on flix.toml.
      - name: Cache toolchain and dependencies
        uses: actions/cache@v4
        with:
          path: |
            .flix
            lib
          key: flix-${{ runner.os }}-${{ hashFiles('flix.toml') }}

      - name: Check
        run: bin/flix check

      - name: Test
        run: bin/flix test

      # The test suite must stay headless: no @Test may open a window or a sound line.
      # Runners have neither a display nor a mixer -- Processing's ShimAWT calls
      # System.exit(1) on HeadlessException, and AudioSystem throws an unchecked
      # IllegalArgumentException when no mixer exists.
      - name: Verify tests are headless
        run: |
          if grep -rn 'Sketch\.start\|Audio\.' test/; then
            echo "::error::test/ must not open a window or an audio device."
            echo "::error::Use Canvas.runCollecting for rendering and assert on World.sounds for audio."
            exit 1
          fi

      - name: Verify formatting
        run: |
          bin/flix format
          if ! git diff --exit-code -- src test; then
            echo "::error::Run 'bin/flix format' and commit the result."
            exit 1
          fi
